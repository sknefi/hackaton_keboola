
export function specifyNewsletter() {
	const specification = "User wants to generate text based on his topic. Generate HTML code that user will display on frontend and return it as a string. We will use this HTML code to display the newsletter on frontend and send it to the users. Please generate the newsletter in the following format: <h1>Topic</h1> <p>Text</p>. Only return the HTML code, nothing else. Please add styles to the newsletter. The styles should be in the following format: <style>...</style>. Only return the HTML code, nothing else."
	const type_specification = "Please generate the newsletter in the following format: <h1>Topic</h1><p>Text</p>. Only return the HTML code, nothing else. Please add styles to the newsletter. The <style> tag should NOT be there in the HTML code. Use style attribute in the HTML tag. Only return the HTML code, nothing else."
	const becareful = "Dont use backslashes in the HTML code (for example in the style attribute style=backslash is forbidden). Display the simpliest HTML, nothing fancy, just the text and styles in the most basic way possible. Do not use any other tags than <h1>, <p>, <div>, <span>. Do not use any other attributes than style. Do not use any other styles than the ones that are already in the HTML code. Be careful with the backslashes in the HTML code, code will not work if you use them inproperly. Write it on one line dont use newlines. Code should be valid HTML code which will work in the browser. The backslash IS FORBIDDEN FOR YOU in the output"
	return specification + type_specification + becareful;
}
